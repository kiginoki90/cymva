<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kobayashi.cymva"> 
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:label="cymva"
        android:name="${applicationName}"
        android:roundIcon="@mipmap/ic_launcher"
        android:icon="@mipmap/ic_launcher_monochrome"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        
        <activity
            android:name=".MyFlutterActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|screenSize|keyboardHidden|keyboard|smallestScreenSize|screenLayout|density|layoutDirection|fontScale|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        
        <!-- Don't delete the meta-data below. -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- 権限の説明文 -->
        <meta-data
            android:name="android.permission.READ_EXTERNAL_STORAGE"
            android:value="このアプリは、ファイルをフォトライブラリに追加するためにアクセスが必要です。例えば、写真やビデオを保存するために使用します。"/>
        <meta-data
            android:name="android.permission.WRITE_EXTERNAL_STORAGE"
            android:value="このアプリは、ファイルをフォトライブラリに追加するためにアクセスが必要です。例えば、写真やビデオを保存するために使用します。"/>
        
        <!-- AdMobのアプリIDを設定 -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-7724581404839169~1418297562"/>
    </application>
    
    <queries>
        <!-- Intent to view http links -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="http" />
        </intent>

        <!-- Intent to view https links -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
        </intent>
        
        <!-- Intent for processing text -->
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>